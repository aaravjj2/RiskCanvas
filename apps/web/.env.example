# Frontend Configuration

# DEMO Mode (optional, defaults to false)
# Set to "true" to enable deterministic demo mode at build time
# Can be overridden at runtime via localStorage
VITE_DEMO_MODE=false

# API Base URL (optional, defaults to http://localhost:8090)
# VITE_API_BASE=http://localhost:8090
